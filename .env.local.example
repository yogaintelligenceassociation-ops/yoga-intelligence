# Local-only frontend overrides.
# Copy to `.env.local` if you want to override REACT_APP_* vars during development.

# Leave blank to call the relative /api path (works on Vercel and via the
# Craco dev-server proxy that forwards /api to your local uvicorn server).
REACT_APP_API_BASE_URL=
